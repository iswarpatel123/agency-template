---
interface Props {
  score: string;
  total: string;
  text: string;
  ratingImageUrl: string;
}

const { score, total, text, ratingImageUrl } = Astro.props;
---

<div class="flex items-center gap-2">
  <img src={ratingImageUrl} alt={`${score} out of 5 stars`} class="h-5" />
  <span class="font-bold text-base text-gray-800">{score}/5</span>
  <span class="font-bold text-sm text-gray-700"> {total}+ {text}</span>
</div>
